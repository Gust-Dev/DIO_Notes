

const entradas = {gets, print } = require('./exercicio12_gets');

const n = gets();

//const entradas = [5,4, 3, 1, 10, 8];
  
for (let i = 0; i < n; i++) {
  const numero = gets();

    if (numero%2 === 0 ) {

        
        
    }
}