const numeroTab = 10;
let mult = 0
for (let i = 0; i < numeroTab; i++) {
    const tabuada = i+1;
    mult = tabuada
    calculo = tabuada*numeroTab
   console.log(`${numeroTab}x${tabuada}=${calculo}`)
}

 

